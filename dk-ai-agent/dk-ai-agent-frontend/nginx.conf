map $http_x_forwarded_proto $forwarded_proto {
    default $http_x_forwarded_proto;
    ""      $scheme;
}

# 咨聊路径不做访问日志（旧客户端可能仍带查询串，且 URL 本身含会话 id）。
map $request_uri $api_access_loggable {
    default                     1;
    ~^/api/ai/counseling/chat/ 0;
}

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    access_log /var/log/nginx/access.log combined if=$api_access_loggable;

    location / {
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 入口 HTML 不缓存：静态资源本身带内容哈希（immutable），唯有 index.html
    # 指向新哈希文件名，缓存它会让发布后手机端仍加载旧版 JS/CSS。
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location ^~ /api/ {
        proxy_pass http://backend:8123/api/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_set_header Connection "";

        # SSE 必须关闭代理缓存，并给深度思考留足读取时间。
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_intercept_errors off;
        add_header X-Accel-Buffering no always;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public, immutable";
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
